package view;

import javax.swing.*;
import java.awt.*;

/**
 * Creates the size for the outer border of the app
 */
public class Border extends JPanel {

    /**
     * defines the border
     */
    public Border() {
        setPreferredSize(new Dimension(30, 30));
    }
}
